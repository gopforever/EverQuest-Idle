# This file intentionally left empty.
# All secrets are stored in Replit Secrets (the lock icon in the sidebar).
# Do NOT add API keys here — this file is tracked by git.
#
# Required secrets (set via Replit Secrets):
#   VITE_AI_GATEWAY_KEY    — Vercel AI Gateway client key (for ghost chat)
#   VITE_SUPABASE_URL      — Supabase project URL (for cloud saves)
#   VITE_SUPABASE_ANON_KEY — Supabase anonymous key (for cloud saves)
